name: Deploy Hexo Blog to My VPS
on:
  pull_request:
  push:
    branches:
      - master
      - hexo

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Clone this repo
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: hexo